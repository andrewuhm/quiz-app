<div class="questionnaires_<%= action_name %>">
<h1>
    Quizzes
</h1>
<%#=
<hr>
  <%= link_to "New Survey", new_survey_path %>

<ul>
  <% @surveys.each do |survey| %>
    <li>
      <%= link_to survey.name, new_quizzes_attempt_path(survey) %>
    </li>
  <% end -%>
</ul>
</div>
